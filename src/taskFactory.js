export default function taskFactory(title, dueDate) {
    // tackle this in local storage
    // addtoproject method?? or something like that
    // do something like, storage(project, {{task1}, {task2} <- add this bit right here})
    return { title, dueDate }
}