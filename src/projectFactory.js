export default function projectFactory(title) {
    const addDescription = () => {
        console.log('h')
    }
    // somehow i feel like this should have a add task to project method
    // tackle this in local storage
    return { title }
}